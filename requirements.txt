# SALOME Documentation RAG System Requirements

# ============================================
# PyTorch (CPU-only to avoid large CUDA packages)
# ============================================
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu                    # CPU-only version (much smaller than CUDA)

# ============================================
# EXTRACTION (process_multi_module_salome_docs.py)
# ============================================
beautifulsoup4==4.14.3          # HTML parsing
markdownify==1.2.3              # HTML -> Markdown (preserves code blocks as fenced text)
chromadb==1.5.5                 # Vector database
transformers==5.3.0            # Token-aware chunking (optional but recommended)

# ============================================
# CHATBOT CORE (core/salome_chatbot.py)
# ============================================
# LangChain components
langchain-core==1.2.22          # Core abstractions (prompts, runnables)
langchain-chroma==1.1.0         # ChromaDB integration
langchain-openai==1.1.12        # OpenAI-compatible API (for LiteLLM)
langchain-huggingface==1.2.1    # HuggingFace embeddings (replaces langchain-community)

# Embeddings
sentence-transformers==5.3.0    # Embedding models (all-MiniLM-L6-v2)
rank_bm25==0.2.2                # BM25 sparse retrieval (hybrid search)

# Agentic-smol mode (optional — only needed if smol_enabled: true in config.json)
smolagents==1.26.0               # CodeAgent multi-hop browsing (chatbot/core/agentic_smol_chatbot.py)

# ============================================
# WEB UI (ui/web.py) - OPTIONAL
# ============================================
gradio==6.10.0                  # Web interface (optional, for --web mode)

# ============================================
# BENCHMARK & EVALUATION
# ============================================
matplotlib==3.8.4               # Graphing and visualization (for --graphs option)

# ============================================
# OPTIMIZER (evaluator/optimizer/optimizer.py)
# ============================================
optuna==4.9.0                   # Bayesian hyperparameter search (TPE sampler + pruning)

# ============================================
# TESTING
# ============================================
pytest==7.4.4                   # Testing framework

# ============================================
# NOTES
# ============================================
# - numpy and other deps are pulled in transitively
# - No need for top-level 'langchain' package (using specific components)
# - openai package is included in langchain-openai
# - matplotlib is optional but recommended for benchmark analysis graphs
